<UserControl x:Class="IPT102PansoyWPF.Views.AddMovieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <!-- Movie Form -->
        <Border Grid.Column="0" Width="380" Padding="20" Background="White" CornerRadius="10"
                VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
            <StackPanel>
                <TextBlock Text="{Binding SaveButtonText, StringFormat='{}{0} Movie'}"
                           FontSize="22" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <TextBlock Text="Title"/>
                <TextBox Height="30" Margin="0,5,0,10" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Release Year"/>
                <TextBox Height="30" Margin="0,5,0,10" Text="{Binding ReleaseYear, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Runtime (minutes)"/>
                <TextBox Height="30" Margin="0,5,0,10" Text="{Binding RuntimeMinutes, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Rating (0.0 - 10.0)"/>
                <TextBox Height="30" Margin="0,5,0,20" Text="{Binding Rating, UpdateSourceTrigger=PropertyChanged}"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Width="80" Margin="0,0,10,0" Command="{Binding CancelCommand}"/>
                    <Button Content="{Binding SaveButtonText}" Width="80" Background="#007ACC"
                            Foreground="White" Command="{Binding SaveCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Movie List -->
        <Border Grid.Column="1" Padding="20" Background="White" CornerRadius="10" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Movie List" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Width="300" Height="30" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <DataGrid Grid.Row="2" ItemsSource="{Binding Movies}" AutoGenerateColumns="False"
                          IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding MovieId}" Width="50"/>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Year" Binding="{Binding ReleaseYear}" Width="60"/>
                        <DataGridTextColumn Header="Runtime" Binding="{Binding RuntimeMinutes}" Width="70"/>
                        <DataGridTextColumn Header="Rating" Binding="{Binding Rating}" Width="60"/>

                        <DataGridTemplateColumn Header="Edit" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Edit" Background="#FFA500" Foreground="White" Padding="8,4"
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Delete" Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Background="#DC143C" Foreground="White" Padding="8,4"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
